# Fred — AI assistant context

The detailed facts and instructions the chat uses are **not published here** on purpose.

The assistant answers from **server-managed context** (Cloudflare Worker + KV). For anything not covered in chat, use the contact options on the site.
